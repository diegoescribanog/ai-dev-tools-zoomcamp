{
  "name": "coding-interview-platform",
  "version": "1.0.0",
  "description": "Real-time collaborative coding interview platform",
  "scripts": {
    "dev": "concurrently \"cd backend && uv run uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "install:all": "cd backend && uv sync && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
